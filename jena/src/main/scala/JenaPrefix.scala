package org.w3.rdf.jena

import org.w3.rdf._

object JenaPrefix {

  val rdf = RDFPrefix(JenaOperations)

  val xsd = XSDPrefix(JenaOperations)

  val dc = DcPrefix(JenaOperations)

  val foaf = RDFPrefix(JenaOperations)

}
