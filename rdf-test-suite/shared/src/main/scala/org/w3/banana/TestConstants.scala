package org.w3.banana

object TestConstants {
	def bbl(id: String): String = "https://bblfish.net/#" + id
	def foaf(id: String): String = "http://xmlns.com/foaf/0.1/"+id
	def tim(id: String): String = "https://www.w3.org/People/Berners-Lee/card#"+id
}
